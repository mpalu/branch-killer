# Branch Killer :smiling_imp:

Let's kill unused git branches locally

## Usage

`./branch-killer.sh $PROJECTS_DIRECTORY`
`$PROJECTS_DIRECTORY` is the container directory where the git projects lives
